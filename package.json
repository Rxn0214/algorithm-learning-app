{
  "name": "algorithm-learning-app",
  "version": "2.0.0",
  "private": true,
  "description": "高中信息技术学习APP - 算法与程序实现",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "cap-init": "npx cap init 算法学习助手 com.algorithm.app --webDir frontend/dist",
    "cap-add-android": "npx cap add android",
    "cap-sync": "npx cap sync",
    "cap-open": "npx cap open android",
    "cap-build": "cd frontend && npm run build && cd .. && npx cap sync && npx cap copy"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0"
  }
}
