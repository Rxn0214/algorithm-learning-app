/**
 * 本地知识库 - 智能体问答引擎
 * 覆盖必修1 第二章全部8个课时知识点
 * 包含丰富的问答内容，支持智能答疑解惑
 */

const KNOWLEDGE = [
  // ===== 课时2：算法的五大特征（最高优先级）=====
  {
    keywords: ['五大特征', '五个特征', '特征', '特性'],
    checkAll: ['算法'],
    answer: `算法有五大特征：

① **有穷性** — 必须在有限步骤内结束，不能无限循环
② **确定性** — 每一步都必须明确无歧义，同样的输入→同样的输出
③ **可行性** — 每一步都是可执行的，通过有限次操作能完成
④ **输入** — 可以没有输入，也可以有多个输入
⑤ **输出** — 至少有一个输出结果

📝 记忆口诀："有确可行，输入输出"

💡 这是考试常考点，建议多理解每个特征的具体含义！`
  },
  {
    keywords: ['有穷性', '有限', '无限循环', '停不下来', '终止', '有限步骤', '死循环'],
    answer: `**有穷性**是算法最重要的特征之一，指算法必须在有限步骤内结束。

🎯 深入理解：
• 有穷性 ≠ 程序运行快，而是保证"一定会结束"
• 算法和程序的区别：程序可以无限运行（如操作系统），但算法必须有限
• 死循环的程序不满足有穷性，不是合法的算法

🔍 正确例子：
• 计算1到100的和 → 最多执行100次加法就结束 ✅
• 查找数组中的某个数 → 找到就停止，最多找n次 ✅

❌ 错误例子：
• "只要天气好就一直输出晴天" → 没有终止条件 ❌
• while(true) { ... } → 无限循环，不满足有穷性 ❌

💡 考试技巧：判断有穷性时，问自己"这个算法什么时候会停下来？"`
  },
  {
    keywords: ['确定性', '歧义', '明确', '二义性', '无歧义', '模糊'],
    answer: `**确定性**要求算法的每一步都必须是明确、无歧义的。

🎯 深入理解：
• 同样的输入 → 经过算法 → 必须得到同样的输出
• 不能出现"大致""大概""可能"这类模糊描述
• 计算机没有"常识"，必须精确告诉它每一步怎么做

🔍 正确例子：
• "把10个苹果分成5个和5个两堆" → 明确 ✅
• "计算两个数的平均值：(a+b)/2" → 明确 ✅

❌ 错误例子：
• "把苹果分成两堆" → 有歧义（怎么分？分多少？）❌
• "取一个较大的数" → 不明确（多大多算大？）❌

💡 实际应用：写算法时，想象你在教一个完全不懂的人，每一步都要说清楚！`
  },
  {
    keywords: ['可行性', '可执行', '能实现', '可操作', '做不到'],
    answer: `**可行性**指算法的每一步都必须在现实中可以执行。

🎯 深入理解：
• 算法描述的操作必须在现实中可以实现
• 不能是理论上的"如果...就能"
• 每一步都要有具体的执行方法

🔍 正确例子：
• 计算全班平均分 → 加总每个人的分数再÷人数 ✅
• 查找最大值 → 逐个比较，记录最大的 ✅

❌ 错误例子：
• "算出全校学生的确切智商" → 无法准确测量 ❌
• "预测明天股价精确值" → 不可能做到 ❌

💡 思考方法：看到算法步骤时，问"这一步具体怎么做？我能写出代码实现吗？"`
  },

  // ===== 课时6：分支结构 =====
  {
    keywords: ['分支', '分支结构', '判断', '条件', 'if', 'else', '如果', '否则', '选择结构', '条件判断'],
    answer: `**分支结构**是程序的三大基本结构之一，根据条件判断结果选择不同的执行路径。

📌 核心语法格式：
if (条件表达式) {
    // 条件为真时执行的代码
} else {
    // 条件为假时执行的代码
}

🔍 经典例子：判断成绩是否及格
if (score >= 60) {
    输出 "及格"
} else {
    输出 "不及格"
}

🎯 重要知识点：
• 条件表达式的结果是布尔值（true或false）
• 可以用比较运算符：> < == >= <= !=
• 可以用逻辑运算符：&&（且）||（或）!（非）
• else部分可以省略，但if必须要有

💡 多分支情况：可以用 if-else if-else 处理多个条件
if (score >= 90) { 输出 "优秀" }
else if (score >= 60) { 输出 "及格" }
else { 输出 "不及格" }`
  },
  {
    keywords: ['比较运算符', '大于', '小于', '等于', '不等于', '运算符'],
    answer: `**比较运算符**用于比较两个值，结果是布尔值（true/false）。

📌 常用比较运算符：
━━━━━━━━━━━━━━━━━━━━━━━━
  >    大于      5 > 3 → true
  <    小于      5 < 3 → false
  ==   等于      5 == 5 → true
  !=   不等于    5 != 3 → true
  >=   大于等于  5 >= 5 → true
  <=   小于等于  3 <= 5 → true
━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ 注意区分：
• = 是赋值（把值存入变量）
• == 是比较（判断是否相等）

💡 组合使用：
• score >= 60 && score < 90 → 60到89分之间
• age > 18 || hasPermission → 成年或有权限`
  },

  // ===== 课时7：循环结构 =====
  {
    keywords: ['循环', '循环结构', '重复', 'for', 'while', '遍历', '迭代', '多次执行'],
    answer: `**循环结构**是程序的三大基本结构之一，用于重复执行某段代码。

📌 两种常用循环形式：

🔄 **for循环** — 已知循环次数时使用
for (初始化; 条件; 更新) {
    // 重复执行的代码
}
适合场景：遍历数组、执行固定次数操作

🔄 **while循环** — 未知循环次数时使用
while (条件) {
    // 重复执行的代码
}
适合场景：等待用户输入、条件满足才停止

🔍 对比选择：
• 知道要循环多少次 → 用for
• 不知道次数，只知道什么时候停 → 用while

⚠️ 重要提醒：循环必须有终止条件，否则会变成死循环！
死循环会导致程序卡住、电脑发热、甚至崩溃。`
  },
  {
    keywords: ['for循环', 'for语句', 'for结构', '计数循环'],
    answer: `**for循环**是最常用的循环结构，适合已知循环次数的情况。

📌 结构详解：
for (初始化; 条件判断; 变量更新) {
    循环体（重复执行的代码）
}

🎯 执行顺序：
① 初始化（只执行一次）
② 条件判断 → 为真继续，为假退出
③ 执行循环体
④ 变量更新
⑤ 回到步骤②继续判断

🔍 经典例子：计算1到100的和
sum = 0
for (i = 1; i <= 100; i++) {
    sum = sum + i
}
// 结果：sum = 5050

💡 常见写法：
• i++ 表示每次加1
• i += 2 表示每次加2
• i-- 表示每次减1`
  },
  {
    keywords: ['while循环', 'while语句', 'while结构', '条件循环'],
    answer: `**while循环**适合未知循环次数的情况。

📌 结构：
while (条件) {
    循环体（只要条件为真就重复执行）
}

🎯 执行逻辑：
• 先检查条件
• 条件为真 → 执行循环体 → 再检查条件...
• 条件为假 → 退出循环

🔍 经典例子：猜数字游戏
answer = 42
guess = 0
while (guess != answer) {
    guess = 输入("猜一个数")
}
输出("猜对了！")

⚠️ 注意事项：
• 循环体内必须改变条件相关的变量
• 否则会变成死循环！
• 建议设置最大循环次数作为保护

💡 while vs for：
• while更灵活，但需要自己管理循环变量
• for更结构化，适合计数场景`
  },
  {
    keywords: ['死循环', '无限循环', '循环不停止', '卡住'],
    answer: `**死循环**是指循环永远不停止，条件永远为真。

⚠️ 死循环的危害：
• 程序卡住，无法继续执行
• 占用CPU资源，电脑发热
• 可能导致程序崩溃

🔍 常见死循环写法：
❌ while(true) { } — 条件永远为真
❌ for(;;) { } — 无终止条件
❌ while(x > 0) { } 但x一直不减 — 变量不更新

✅ 正确做法：
• 循环条件要有变为假的可能
• 循环体内要更新相关变量
• 可以设置计数器作为保护：
count = 0
while (条件 && count < 1000) {
    // 循环体
    count++
}

💡 调试技巧：如果程序运行很久没反应，检查是否有死循环！`
  },

  // ===== 课时4：流程图 =====
  {
    keywords: ['流程图', 'flowchart', '画流程图', '符号', '图形', '起止框', '处理框', '判断框', '菱形', '矩形', '椭圆', '平行四边形'],
    answer: `**流程图**是算法的图形化表示，用特定符号描述执行过程。

📌 常用符号：
━━━━━━━━━━━━━━━━━━━━━━━━
  椭圆 ○     起止框（开始/结束）
  矩形 □     处理框（计算/操作）
  菱形 ◇     判断框（条件分支）
  平行四边形 ▱ 输入输出框
  箭头 →    流程线（执行方向）
━━━━━━━━━━━━━━━━━━━━━━━━

🔍 画流程图的步骤：
① 确定输入和输出
② 分析处理步骤
③ 确定判断条件
④ 用符号画出流程
⑤ 用箭头连接

💡 画图技巧：
• 先画主干，再补充细节
• 判断框有两个出口（是/否）
• 流程线不要交叉
• 保持从上到下、从左到右的方向

📝 考试常考：根据流程图写出代码，或根据代码画出流程图`
  },
  {
    keywords: ['流程线', '箭头', '执行顺序', '执行方向'],
    answer: `**流程线**（箭头→）表示算法的执行方向。

• 流程线连接各个符号，决定执行路径
• 必须标明方向，不能没有指向
• 流程线上可以有文字说明条件

💡 画流程图时注意：流程线不要交叉，保持清晰有序！`
  },

  // ===== 课时3：算法的描述 =====
  {
    keywords: ['描述方法', '描述方式', '表示方法', '自然语言', '伪代码', '类代码'],
    checkAll: ['算法'],
    answer: `描述算法有**三种常用方法**：

① **自然语言** 🗣️
   用日常语言描述步骤
   • 优点：通俗易懂
   • 缺点：可能有歧义，不够精确

② **流程图** 📊
   用图形符号表示流程
   • 优点：直观清晰，逻辑一目了然
   • 缺点：画起来费时

③ **伪代码** 💻
   介于自然语言和编程语言
   • 优点：结构清晰，易转代码
   • 缺点：需要编程基础

💡 推荐学习顺序：
自然语言想思路 → 流程图理逻辑 → 伪代码写结构 → 代码实现`
  },
  {
    keywords: ['伪代码'],
    answer: `**伪代码**是介于自然语言和编程语言之间的描述方式。

📌 特点：
• 使用编程的逻辑结构（顺序、分支、循环）
• 语法自由，不依赖具体编程语言
• 结构清晰，容易转换成真实代码

🔍 伪代码示例：判断偶数
输入：整数 n
如果 n % 2 = 0 则
    输出 "是偶数"
否则
    输出 "是奇数"

💡 推荐在写代码前先用伪代码列提纲！`
  },

  // ===== 课时5：顺序结构 =====
  {
    keywords: ['顺序结构', '顺序', '依次', '逐条', '按顺序', '直线执行'],
    answer: `**顺序结构**是最简单的程序结构，语句按书写顺序依次执行。

📌 特点：
• 从第一条语句开始，逐条执行到最后一条
• 没有分支，没有跳转
• 执行路径是一条直线

🔍 代码示例：
输入 a
输入 b
sum = a + b
输出 sum
执行顺序：①输入a → ②输入b → ③计算 → ④输出

💡 顺序结构是基础：
• 所有程序都从顺序结构开始
• 分支和循环是在顺序基础上增加灵活性`
  },

  // ===== 课时8：程序实现 =====
  {
    keywords: ['程序实现', '编程', '调试', 'debug', '断点', '错误', 'bug', '写代码', '实现'],
    answer: `**程序实现**是将算法用编程语言写成可执行代码的过程。

📌 三大结构组合：
任何程序都可以由**顺序 + 分支 + 循环**三种结构组合实现。

🔍 调试技巧：
① 输出中间结果 — 在关键位置打印变量值
② 设置断点 — 让程序在指定位置暂停检查
③ 分步执行 — 一行一行运行，观察变化
④ 边界测试 — 测试极端情况（如输入0、负数）

🔍 常见错误类型：
• 语法错误：代码写错，程序无法运行
• 逻辑错误：代码能运行，但结果不对
• 运行错误：运行时出现问题（如除以零）

💡 调试心态：调试不是失败，是编程的正常部分！`
  },
  {
    keywords: ['语法错误', '编译错误', '代码写错', '报错'],
    answer: `**语法错误**是代码写错了格式，程序无法运行。

🔍 常见语法错误：
• 漏写分号 ;
• 漏写括号 {} ()
• 关键字拼写错误
• 变量名不合法

📌 特点：
• 程序还没运行就报错
• 错误信息会指出哪一行有问题

💡 解决方法：仔细看错误信息，检查对应行的代码。`
  },
  {
    keywords: ['逻辑错误', '结果不对', '算错了', '错误结果'],
    answer: `**逻辑错误**是代码能运行，但结果不正确。

🔍 常见逻辑错误：
• 条件判断写反
• 循环次数不对
• 计算公式错误
• 变量用错

📌 特点：
• 程序不报错，正常运行
• 但结果和预期不符

💡 解决方法：
① 用简单数据手动计算正确结果
② 在代码中输出中间值，对比分析`
  },

  // ===== 课时1：算法的概念（通用定义）=====
  {
    keywords: ['算法', '概念', '定义', '是什么', '什么叫', '什么是', '含义'],
    answer: `**算法**是解决特定问题的一系列明确的操作步骤和方法。

📌 核心理解：
• 算法就是"怎么做"的方法和步骤
• 同一个问题可以有多种不同的算法
• 算法不依赖具体的编程语言

🔍 生活类比：
• 做菜的食谱就是"做菜的算法"
• 导航路线就是"到达目的地的算法"
• 数学公式就是"计算的算法"

💡 例子对比：计算1+2+3+...+100
方法一：依次相加，加100次
方法二：高斯公式 (1+100)×100÷2 = 5050
两种都是算法，但效率不同！`
  },

  // ===== 综合知识 =====
  {
    keywords: ['变量', '赋值', '存储', '数据类型'],
    answer: `**变量**是用来存储数据的"容器"。

📌 核心概念：
• 变量有名字（标识符）和值
• 变量的值可以改变
• 不同变量存储不同类型的数据

🔍 常见数据类型：
整数 int    如：age = 16
小数 float  如：price = 9.99
字符串 str  如：name = "小明"
布尔 bool   如：isPass = true

💡 变量命名规范：
• 见名知意：用有意义的名字
• 不能用数字开头
• 不能用关键字`
  },
  {
    keywords: ['数组', '列表', '集合', '多个数据'],
    answer: `**数组**是存储多个相同类型数据的容器。

📌 特点：
• 一个数组可以存多个数据
• 通过索引（位置）访问每个元素
• 索引从0开始

🔍 示例：
scores = [85, 90, 78, 92, 88]
scores[0] = 85  // 第一个
scores[4] = 88  // 第五个

💡 常用操作：
• 遍历数组：for循环逐个访问
• 查找最大值：逐个比较
• 求平均值：先加总再除以个数`
  },

  // ===== 学习方法 =====
  {
    keywords: ['怎么学', '如何学', '学习方法', '计划', '安排', '建议', '指导', '学习路径'],
    answer: `我来帮你规划学习路径 📚

📌 建议学习顺序：
① 课时1-2：理解算法的概念和五大特征
② 课时3-4：学会算法的描述方法，特别是流程图
③ 课时5-7：掌握顺序、分支、循环三种结构
④ 课时8：综合应用，调试程序

💡 每课时学习步骤：
1. 看知识点讲解 → 理解概念
2. 看示例 → 学会应用
3. 做练习题 → 检验理解
4. 有疑问问智能体 → 解决困惑

🎯 学习建议：
• 不要只看不练，要多动手
• 流程图一定要自己画几次
• 遇到问题及时问，不要拖着`
  },
  {
    keywords: ['考试', '考点', '重点', '必考', '复习', '备考'],
    answer: `我来帮你梳理考试重点！🎯

📌 高频考点：
⭐⭐⭐ 必考重点：
• 算法的五大特征
• 流程图符号的含义和画法
• 三种基本结构的识别

⭐⭐ 常考内容：
• for循环和while循环的区别
• 分支结构if-else的使用
• 流程图转代码或反过来

💡 备考建议：
1. 熟记五大特征，能举例说明
2. 多画几个流程图
3. 练习基本代码写法`
  },
  {
    keywords: ['题目', '习题', '作业', '不会做', '答案', '请教', '解题'],
    answer: `我来帮你分析解题思路！💡

📌 解题步骤：
① 理解题意 — 输入什么？输出什么？
② 设计算法 — 用哪种结构？
③ 写代码 — 注意语法
④ 验证测试 — 用数据检验

🔍 你可以：
• 把题目发给我，我帮你分析
• 告诉我你的想法，我帮你找问题

💡 提醒：自己先尝试，收获更大！`
  },

  // 默认
  {
    keywords: [],
    answer: null
  }
]

const GUIDER_PREFIX = '😊 同学你好～'
const TUTOR_PREFIX = '让我来帮你分析一下 🤔'

/**
 * 根据问题查找知识库答案
 */
export function findAnswer(question, lessonId = null) {
  if (!question || !question.trim()) return null

  const q = question.toLowerCase().trim()

  for (const entry of KNOWLEDGE) {
    if (entry.answer === null) continue

    if (entry.checkAll) {
      const allMatch = entry.checkAll.every(kw => q.includes(kw.toLowerCase()))
      if (!allMatch) continue
    }

    const match = entry.keywords.some(kw => q.includes(kw.toLowerCase()))
    if (match) {
      return entry.answer
    }
  }

  return null
}

/**
 * 生成本地回复（智能体核心功能）
 */
export function generateLocalReply(question, agentType = 'tutor', lessonId = null) {
  // 安全检查
  if (!question || typeof question !== 'string') {
    question = ''
  }

  const q = question.trim()

  // 空问题
  if (!q) {
    if (agentType === 'guider') {
      return `${GUIDER_PREFIX} 你想问什么呀？可以直接输入你的问题，我来帮你解答～`
    }
    return `${TUTOR_PREFIX} 请告诉我你的问题，我来帮你分析。`
  }

  // 知识库匹配
  const answer = findAnswer(q, lessonId)

  if (answer) {
    if (agentType === 'guider') {
      return `${GUIDER_PREFIX} ${answer}`
    }
    return `${TUTOR_PREFIX}\n\n${answer}`
  }

  // 无匹配时的智能回复
  const hasQuestionMark = q.includes('?') || q.includes('？')

  if (agentType === 'guider') {
    if (hasQuestionMark) {
      return `${GUIDER_PREFIX} 关于「${q}」，这是个很好的问题！

建议你先看看相关的课时内容，理解基础概念。如果还是不清楚，可以：
• 告诉我具体是哪个课时的问题
• 把课本里的相关内容发给我

学习就是不断提问的过程，加油！💪`
    }
    return `${GUIDER_PREFIX} 「${q}」是学习中的重要内容。

建议你按这个步骤：
① 先通读课本对应章节
② 看看课时里的知识点讲解
③ 做几道练习题巩固理解

需要我帮你规划更具体的学习计划吗？`
  }

  // 辅智风格
  if (hasQuestionMark) {
    return `${TUTOR_PREFIX}

关于「${q}」这个问题，我们可以这样分析：

① 首先确认这个知识点属于哪个课时
② 回顾相关的概念和定义
③ 用具体例子来理解

你能告诉我你现在学到哪个课时了吗？这样我可以更针对性地帮你讲解！`
  }

  return `${TUTOR_PREFIX}

让我帮你分析「${q}」：

这个内容涉及到算法与程序实现的核心知识。建议：

① 先确认你学过哪些相关课时
② 回顾课本上的定义和例子
③ 如果有具体题目，可以发给我看

你目前的学习进度是怎样的？我可以根据你的情况给出更具体的建议。`
}